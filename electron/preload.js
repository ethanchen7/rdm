// Intentionally empty: the window loads RDm's own served page, which needs
// no Electron-specific APIs — it already works standalone in a browser.
// Kept as a placeholder in case a contextBridge API is needed later.
